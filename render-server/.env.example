# Render Server Configuration

# Server Configuration
NODE_ENV=development
PORT=3000

# CORS Configuration (comma-separated origins)
CORS_ORIGIN=chrome-extension://*,http://localhost:*,https://*.render.com

# Rate Limiting
RATE_LIMIT_WINDOW=900000  # 15 minutes in milliseconds
RATE_LIMIT_MAX=100        # Max requests per window

# WebSocket Configuration
WS_HEARTBEAT_INTERVAL=30000   # 30 seconds
WS_CONNECTION_TIMEOUT=60000   # 60 seconds

# Authentication (optional)
API_KEY=your-secret-api-key-here

# Production Settings for Render.com:
# NODE_ENV=production
# CORS_ORIGIN=*
# API_KEY=your-production-api-key

# Note: OLLAMA_SERVER_URL is not needed for Render deployment
# since Ollama servers connect TO this server (reverse connection)
